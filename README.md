# computer-vision-2
This code builds an **image pyrami using `cv2.pyrDown()` to create progressively lower-resolution versions of the same image. It then applies Canny edge detectionateach pyramid level to show how edges change with image scale.
